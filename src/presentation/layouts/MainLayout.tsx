import { Text, View } from 'react-native';

interface Props {
    title: string;
    subTitle?: string;

}

export const MainLayout = () => {
    return (
        <View>
            <Text></Text>
        </View>
    );
}
